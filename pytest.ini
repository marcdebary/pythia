[pytest]
# Die Module liegen unter app/, damit sie im Abbild direkt auf /app landen und
# der Import `from lib import ...` drinnen wie draussen gleich aussieht.
pythonpath = app
testpaths = tests
addopts = -q
